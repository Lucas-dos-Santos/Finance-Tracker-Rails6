<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "FinanceTracker", root_path, class: "navbar-brand text-primary fs-4 px-0 py-0 fw-bold" %>
    <% if user_signed_in? %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "Meu Portifolio", my_portfolio_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Amigos", friends_path, class: "nav-link"%>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Perfil
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li>
                <%= link_to current_user, class: "dropdown-item" do %>
                  <%= fa_icon 'eye' %>  Ver 
                <% end %>
              </li>
              <li>
                <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
                  <%= fa_icon 'edit', class: 'text-danger' %>  Editar
                <% end %>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete, class: "dropdown-item" do %>
                  <%= fa_icon 'sign-out', class: "text-primary" %>
                  Logout
                <% end %>
              </li>
            </ul>
          </li>
        </ul>
        <div class="nav-link text-dark" >
          <%= fa_icon 'user-circle'%>
          <%= current_user.full_name %>
        </div>
      </div>
    <% end %>
  </div>
</nav>
